http:
  middlewares:

    default-chain:
      chain:
        middlewares:
          - security-headers

    infra-chain:
      chain:
        middlewares:
          - security-headers
          - traefik-auth

    security-headers:
      headers:
        frameDeny: true
        contentTypeNosniff: true
        referrerPolicy: "same-origin"
    
    traefik-auth:
      basicAuth:
        users:
          - '{{ env "SVC_DASHBOARD_HASH" }}'
