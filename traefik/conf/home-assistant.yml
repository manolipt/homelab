http:
  routers:
    homeassistant:
      rule: Host(`home.{{ env "DOMAIN" }}`)
      service: homeassistant
      entryPoints:
        - web
  services:
    homeassistant:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:8123"
